name: Test Telegram

on:
  workflow_dispatch:

jobs:
  send:
    runs-on: ubuntu-latest
    steps:
      - name: Send Test
        env:
          BOT_TOKEN: ${{ secrets.TG_BOT_TOKEN }}
          CHAT_ID: "@netralnews_2026"
        run: |
          curl -X POST "https://api.telegram.org/bot$BOT_TOKEN/sendMessage" \
          -d chat_id=$CHAT_ID \
          -d text="TEST WORKFLOW FIX"
